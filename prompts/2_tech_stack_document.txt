Определи стек и создай файл tech_stack_document.mdc  
(alwaysApply: true, globs: ["**/*"]).

Для каждого слоя укажи:
- название библиотеки / сервиса с версией,
- одну строку зачем он нужен,
- ссылку на официальную документацию.

Слои: Runtime, Bot SDK, OpenAI SDK, Database (если нужна), Deployment (Railway), Линтер/Форматтер.
Выведи в Markdown-таблицу.